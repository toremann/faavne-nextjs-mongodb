'use client';

import Container from '../components/Container';
import PageHeader from '../components/PageHeader';

const Portfolio = () => {
  return (
    <Container>
      <PageHeader title={'Portfolio'} subtitle={'Se dine planlagte utbytter'} />
    </Container>
  );
};
export default Portfolio;
